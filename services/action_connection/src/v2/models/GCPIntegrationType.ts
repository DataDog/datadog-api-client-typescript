import { UnparsedObject } from "@datadog/datadog-api-client";

/**
 * The definition of the `GCPIntegrationType` object.
 */
export type GCPIntegrationType = typeof GCP | UnparsedObject;
export const GCP = "GCP";
